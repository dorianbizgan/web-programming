name:Dorian Bizgan
eid:dab4567
bitbucketid:dorianab
comments:pip install was a bit strange with zshell on Mac so I had to install requirements using python3 -m pip install -r requiements which worked as far as I can tell. 
